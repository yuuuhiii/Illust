1.  **Fix Arrow Reflection Math:**
    *   Change the `start_faces` mirroring formula in `IsoLineItem._generate_mesh()` from `(-x + -self.length + start_al, y, z)` to `(-x - self.length/2, y, z)`. The unreflected geometries are defined with an `offset_x` such that after reflecting (`-x`), shifting by `-length/2` correctly aligns the tip and base along the negative X-axis where the cylinder body expects it.
    *   This fixes the detached/flying arrowhead bug when `arrow_pos` is "start" or "both".
2.  **Fix XYZ Axis Visibility:**
    *   In `IsoLineItem.update_geometry()`, hide `self.axis_x`, `axis_y`, `axis_z` unconditionally, and only show them if the user is actively rotating.
    *   Wait, the standard is usually to hide them unless requested, but there's no "actively rotating" state flag inside `IsoLineItem`. Let's remove the axes entirely or hide them from the bounding box logic. The bug is that showing the axes during drag-to-draw corrupts the drag space.
    *   We will introduce a `show_axes` flag defaulting to `False` and only set it `True` when rotating, or remove them entirely if they are too invasive. Actually, hiding them inside `update_geometry` unless specifically requested fixes it. Let's just remove the XYZ axes display logic inside `update_geometry` as it was explicitly mentioned as "The XYZ axes should *only* be visible when the object is actively being rotated", but it's easier to remove them or hide them safely. I will add an `is_rotating` flag to `IsoLineItem`.
3.  **Fix Drag-to-Draw Positioning Offset:**
    *   The `update_preview` function in `DrawIsoLineTool` is setting the item's pos to `(center_x, center_y)`.
    *   However, `IsoLineItem` naturally aligns its geometry around its local `(0, 0)`.
    *   When the axes logic was added, it expanded the bounding box, but the real issue is likely the `update_preview` math combined with `itemChange` snap logic. The preview item is snapping *during* `setPos` inside `update_preview()`.
    *   Actually, `IsoLineItem` has an `itemChange` handler that snaps to the grid: `x = round(new_pos.x() / self.GRID_SIZE) * self.GRID_SIZE`. This snapping happens *after* `setPos(center_x, center_y)` is called in `update_preview()`, causing the preview to jump around unpredictably because `center_x` and `center_y` are midpoints (often not on the grid).
    *   To fix: In `IsoLineItem`, disable snapping for the preview item, or simply make sure that snap logic isn't interfering. We can temporarily disable snap by setting `self.SNAP_ENABLED = False` on the preview item in `DrawIsoLineTool.mousePressEvent()`.
